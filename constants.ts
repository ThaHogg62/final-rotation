import { Video, Comment } from './types';

// The data for videos and comments has been moved to the Node.js backend (server.js).
// The frontend now fetches this data via the API service in `services/api.ts`.
// This file is kept for potential future constants, but the mock data is removed.
